/**
 * @file
 * Custom Elearning behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.customElearning = {
    attach(context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
